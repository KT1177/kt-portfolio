export interface MediaAsset { key: string; alt: string; width: number; height: number; }
// Add actual R2 object keys here when the media library is ready.
export const media: Record<string, MediaAsset> = {};
